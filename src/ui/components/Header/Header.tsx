import { FC } from "react";
import { StyledDefaultHeader } from "./Header.styles";

export const Header: FC = () => {
  return (
    <StyledDefaultHeader>
      <nav></nav>
    </StyledDefaultHeader>
  );
};
